# 智行·端析：多智能体编排与研究基线约束

## 0. 适用范围与默认沟通

- 本文件适用于本仓库中的所有任务；默认使用中文沟通，代码注释遵循项目既有约定。
- 本项目不得调用或依赖任何 `superpowers:*` skill，除非用户之后明确撤销该限制。
- 影响里程碑结论、环境状态或可复现性的命令、测试与结论，均须保留可追溯的验证留痕（命令、环境、输入、输出、日志位置、结论）。
- 同一文件同一时刻只允许一个执行者写入；并行工作必须按文件边界拆分，写入前明确所有者。

## 1. 当前唯一里程碑

当前唯一里程碑是：**跑通 NVIDIA 官方原始 Alpamayo 模型测试并建立可复现基线**。

在该里程碑通过验收门禁前，禁止实施或合入下列优化与部署工作：

- 模型剪枝、蒸馏、量化；
- 视觉 Token 剪枝；
- ONNX 导出或推理；
- TensorRT 构建、转换或推理。

不得根据项目概述、二手材料或命名习惯臆测模型结构、输入输出、预处理、权重格式或推理流程。所有此类事实必须以 NVIDIA 官方源码、官方文档及官方 checkpoint 的实际内容为准，并记录引用位置与验证证据。

## 2. 角色与职责

| 角色 | 模型与思考等级 | 主要职责 | 权限边界 |
| --- | --- | --- | --- |
| 协调者 | 当前主智能体（GPT-5.6 Sol 系列） | 拆分任务、维护里程碑与文件所有权、调度和升级、汇总证据 | 不以协调替代高风险技术决策或验收 |
| 决策者 | GPT-5.6 Sol 系列（high） | 制定研究路线、技术方案、失败归因、风险判断和验收标准 | 对高风险任务给出可执行决策单，并完成最终验收 |
| 执行者 | GPT-5.6 Sol 系列（high） | 按决策单修改、配置、运行、排障、测试并提交可复核执行回报 | 不擅自改变研究路线、扩大范围或执行不可逆操作 |

## 3. 工作流与升级规则

### 3.1 高风险流程

涉及模型来源与版本选择、checkpoint 使用、环境核心依赖、训练或推理链路、数据与评测口径、性能结论、不可逆操作或研究路线的任务，必须遵循：

`GPT-5.6 Sol high 决策 → GPT-5.6 Sol high 执行 → GPT-5.6 Sol high 验收`

执行者仅可执行已经明确的决策单；如执行中发现前提不成立，先停止在安全边界并升级给 Sol，不得自行替换方案。

### 3.2 简单、低风险任务的顺延

仅限机械复现、无研究路线影响、可逆且不涉及同文件并发写的**最简单**低风险任务，可分配给 GPT-5.6 Luna（max）。其余任务一律使用 GPT-5.6 Sol 系列。

`GPT-5.6 Luna max → GPT-5.6 Sol high`

- 若 Luna 不可用，或 Luna 对最简单任务首次执行失败，立即升级至 GPT-5.6 Sol high。

除上述最简单任务外，或出现以下任一情况时，立即由 GPT-5.6 Sol high 处理，不再下放：

- 官方来源、版本、代码或 checkpoint 之间存在冲突；
- 任务范围、验收口径或依赖版本发生变化；
- 需要不可逆操作；
- 可能影响研究路线、模型结构判断、实验结论或后续优化方向。

## 4. 记录模板

### 4.1 决策单（Sol 必填）

- 决策编号、日期、决策者；
- 目标及其对应的当前里程碑；
- 事实依据：官方源码、官方文档、checkpoint 的链接/路径/版本/提交或哈希；
- 任务范围、明确不做的事项、风险等级；
- 执行步骤、允许的命令或变更边界、文件所有者；
- 验收标准、需要保留的日志与产物；
- 回滚或停止条件，以及升级条件。

### 4.2 执行回报（执行者必填）

- 对应决策编号、执行者、开始和结束时间；
- 实际环境：开发机或 Jetson、操作系统、GPU/Jetson 型号、驱动、CUDA、Python、关键依赖版本；
- 实际变更文件及摘要，或明确“无文件变更”；
- 执行命令、输入、checkpoint 标识、输出、退出码与完整日志位置；
- 测试结果与验收标准逐项对照；
- 偏差、失败尝试、已知限制、是否触发升级；
- 可复现步骤及建议的下一步。

## 5. NVIDIA 官方 Alpamayo 模型跑通验收门禁

官方测试必须优先原样运行 NVIDIA 官方入口与官方样例。只有当官方入口存在已记录的环境阻塞，且 Sol 已事前批准时，才可进行最小适配的兼容性测试；该兼容性测试不得据此宣称“官方测试跑通”。

开发机官方原始模型基线成功是后续任何阶段的必要前提：在不改造模型结构、不使用第 1 节禁止项的条件下，使用经核验的 NVIDIA 官方原始源码与官方 checkpoint，原样完成官方入口与官方样例测试并留存证据。

### 5.1 开发机门禁

开发机验收至少应证明：

- 官方仓库、提交/版本、checkpoint 来源与校验信息已记录；
- 依赖环境可从记录复现，官方导入、权重加载和预处理均成功；
- 原样使用官方入口与官方样例完成端到端测试，进程退出码为 0；
- 输出的类型、形状、关键字段及合理性与官方代码或示例相符；
- 已保存原始命令、完整日志、环境清单、输入标识和输出产物位置；
- Sol 根据决策单完成复核验收。

### 5.2 Jetson 实机门禁

Jetson 验收不得用开发机结果替代。开发机官方原始模型基线成功后，必须在目标 Jetson 实机尝试官方原始模型，并将结果独立记录：

- 实机型号、JetPack/L4T、CUDA、驱动、功耗模式与存储/内存条件已记录；
- 记录实际设备日志、退出码、资源观测（至少内存与显存/统一内存）及端到端耗时；
- 若成功从官方原始模型与官方 checkpoint 加载并完成同一口径的端到端测试，方可称为“Jetson 实机跑通”；
- 若可复现实验证明 OOM、ARM64 限制或官方链路不兼容，必须记录阻塞证据、复现步骤与资源观测。Sol 可验收为“Jetson 原始模型不可运行基线”并批准下一阶段优化，但不得称为“Jetson 实机跑通”。

只有开发机官方原始模型基线成功，并且 Jetson 实机结果已按本节记录且由 Sol 验收（“Jetson 实机跑通”或“Jetson 原始模型不可运行基线”）后，才允许提出后续剪枝、蒸馏、量化、视觉 Token 剪枝、ONNX 或 TensorRT 的独立决策单。
